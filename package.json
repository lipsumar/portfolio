{
  "devDependencies": {
    "cssnano": "^4.1.10"
  },
  "scripts": {
    "build": "parcel build index.html",
    "start": "cd dist && static-server --port 3000"
  },
  "dependencies": {
    "parcel-bundler": "^1.12.4",
    "static-server": "^2.2.1"
  }
}
